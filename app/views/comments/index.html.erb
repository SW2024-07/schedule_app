<h1>コメント一覧</h1>

<%= link_to '新しいコメントを書く', new_comment_path %>

<ul>
  <% @comments.each do |comment| %>
    <li>
      <p><%= comment.content %></p>
    </li>
  <% end %>
</ul>
<%= link_to 'Back', request.referer || posts_path %>
<%= link_to '一覧へ', user_calendars_path%>